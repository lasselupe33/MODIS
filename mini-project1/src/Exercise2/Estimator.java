package Exercise2;

public class Estimator {
    public Estimator() {

    }

    public static void estimate(int datagramSize, int amount, int interval) {
        // Send datagrams with overhead
        // has to have an id (autoincremented?)
        // We need a timeout as well

        // Send all datagrams with interval as pause


        // Analyse what we recieved
    }
}
